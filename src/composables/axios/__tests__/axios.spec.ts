import { describe, it } from 'vitest'

describe('useAxios Composable', () => {
  describe('HTTP GET', () => {
    it.todo('facilitates GET requests')
  })

  describe('HTTP POST', () => {
    it.todo('facilitates POST requests')
  })
})
